services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/url_shortener_app/data
    environment:
      FLASK_APP: core
      DATABASE_URL: sqlite:///data/shorty.db
    env_file:
      - .env